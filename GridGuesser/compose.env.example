# Copy to compose.env and adjust. Used only for docker compose variable substitution
# (image names and host ports), not passed into containers:
#   docker compose --env-file compose.env -f docker-compose.prod.yml up -d

GHCR_OWNER=yourgithubuser
IMAGE_TAG=latest
BACKEND_HOST_PORT=3001
FRONTEND_HOST_PORT=3000
